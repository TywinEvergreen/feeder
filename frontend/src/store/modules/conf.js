const state = {
    default_datetime_format: 'HH:mm DD/MM/YYYY',
};

const getters = {
};

const mutations = {
};

const actions = {
};

export default {
    state,
    getters,
    mutations,
    actions,
};